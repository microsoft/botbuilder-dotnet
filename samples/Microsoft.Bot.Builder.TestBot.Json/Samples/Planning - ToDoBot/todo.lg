# ShowTodo
- CASE: {count(user.todos) > 0}
    - ```
Your most recent @{count(user.todos)} tasks are 
 @{humanize(user.todos, '[showSingleTodo]', '\n')}
      ```
- DEFAULT:
    - You don't have any tasks.

# showSingleTodo(x)
- * {x}