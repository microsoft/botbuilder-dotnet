[import from base lg 1](AliasBase1.lg) as base
[import from base lg 2](AliasBase2.lg) as base2

# welcome(theName)
- ${base.welcome(theName)}

# welcome2(theName)
- ${base2.welcome(theName)}

# callLength
- ${length('name')}

# callBase1Length
- ${base.length()}

# callBase2Length
- ${base2.length()}