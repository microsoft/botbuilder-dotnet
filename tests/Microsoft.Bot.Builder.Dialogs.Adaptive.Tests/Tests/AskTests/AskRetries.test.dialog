using System;
using System.Collections.Generic;
using System.ComponentModel;
using System.Diagnostics;
using System.Text;

namespace Microsoft.Bot.Builder.Dialogs.Adaptive.Tests.Tests.ActionTests
{
    public partial class Action_AskRetries: Component
    {    
        public Action_AskRetries()
        {
            InitializeComponent();
        }

        public Action_AskRetries(IContainer container)
        {
            container.Add(this);

            InitializeComponent();
        }
    }
}
