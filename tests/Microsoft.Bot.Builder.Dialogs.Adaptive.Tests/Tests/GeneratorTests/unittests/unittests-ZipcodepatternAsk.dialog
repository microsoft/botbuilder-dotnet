
{
    "$schema": "https://raw.githubusercontent.com/microsoft/botbuilder-dotnet/chrimc/form/schemas/sdk.schema",
    "$kind": "Microsoft.OnEndOfActions",
    "condition":"!$Zipcodepattern && contains(dialog.requiredProperties, 'Zipcodepattern')",
    "actions": [
        {
            "$kind": "Microsoft.Ask",
            "activity": "@{AskZipcodepattern()}",
            "expectedProperties": [
                "Zipcodepattern"
            ]
        },
    ]
}
