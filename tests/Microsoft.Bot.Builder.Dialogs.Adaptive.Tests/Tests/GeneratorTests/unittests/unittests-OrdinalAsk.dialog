
{
    "$schema": "https://raw.githubusercontent.com/microsoft/botbuilder-dotnet/chrimc/form/schemas/sdk.schema",
    "$kind": "Microsoft.OnEndOfActions",
    "condition":"!$Ordinal && contains(dialog.requiredProperties, 'Ordinal')",
    "actions": [
        {
            "$kind": "Microsoft.Ask",
            "activity": "@{AskOrdinal()}",
            "expectedProperties": [
                "Ordinal"
            ]
        },
    ]
}
