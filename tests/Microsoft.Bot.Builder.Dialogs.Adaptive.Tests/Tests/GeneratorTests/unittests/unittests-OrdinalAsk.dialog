
{
    "$schema": "https://raw.githubusercontent.com/microsoft/botbuilder-dotnet/master/schemas/sdk.schema",
    "$kind": "Microsoft.OnEndOfActions",
    "condition":"!$Ordinal",
    "priority": "indexOf(dialog.requiredProperties, 'Ordinal')",
    "actions": [
        {
            "$kind": "Microsoft.Ask",
            "activity": "@{AskOrdinal()}",
            "expectedProperties": [
                "Ordinal"
            ]
        }
    ]
}
