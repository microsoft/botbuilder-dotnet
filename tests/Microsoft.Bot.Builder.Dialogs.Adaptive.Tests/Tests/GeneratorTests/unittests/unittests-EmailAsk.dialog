
{
    "$schema": "https://raw.githubusercontent.com/microsoft/botbuilder-dotnet/chrimc/form/schemas/sdk.schema",
    "$kind": "Microsoft.OnAsk",
    "condition":"!$Email && contains(dialog.requiredProperties, 'Email')",
    "actions": [
        {
            "$kind": "Microsoft.Ask",
            "activity": "@{AskEmail()}",
            "expectedProperties": [
                "Email"
            ]
        },
    ]
}
