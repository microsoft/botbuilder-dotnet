
{
    "$schema": "https://raw.githubusercontent.com/microsoft/botbuilder-dotnet/chrimc/form/schemas/sdk.schema",
    "$kind": "Microsoft.OnEndOfActions",
    "condition":"!$Keyphraseproperty && contains(dialog.requiredProperties, 'Keyphraseproperty')",
    "actions": [
        {
            "$kind": "Microsoft.Ask",
            "activity": "@{AskKeyphraseproperty()}",
            "expectedProperties": [
                "Keyphraseproperty"
            ]
        },
    ]
}
