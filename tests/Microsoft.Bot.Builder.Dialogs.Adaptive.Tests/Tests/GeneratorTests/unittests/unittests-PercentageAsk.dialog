
{
    "$schema": "https://raw.githubusercontent.com/microsoft/botbuilder-dotnet/chrimc/form/schemas/sdk.schema",
    "$kind": "Microsoft.OnEndOfActions",
    "condition":"!$Percentage && contains(dialog.requiredProperties, 'Percentage')",
    "actions": [
        {
            "$kind": "Microsoft.Ask",
            "activity": "@{AskPercentage()}",
            "expectedProperties": [
                "Percentage"
            ]
        },
    ]
}
