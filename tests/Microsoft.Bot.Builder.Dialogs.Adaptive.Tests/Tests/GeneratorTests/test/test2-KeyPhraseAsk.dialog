
{
    "$schema": "https://raw.githubusercontent.com/microsoft/botbuilder-dotnet/chrimc/form/schemas/sdk.schema",
    "$kind": "Microsoft.OnAsk",
    "condition":"!$KeyPhrase && contains(dialog.requiredProperties, 'KeyPhrase')",
    "actions": [
        {
            "$kind": "Microsoft.Ask",
            "activity": "@{AskKeyPhrase()}",
            "expectedProperties": [
                "KeyPhrase"
            ]
        },
    ]
}
