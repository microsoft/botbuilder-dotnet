
{
    "$schema": "https://raw.githubusercontent.com/microsoft/botbuilder-dotnet/chrimc/form/schemas/sdk.schema",
    "$kind": "Microsoft.OnAsk",
    "condition":"!$Location && contains(dialog.requiredProperties, 'Location')",
    "actions": [
        {
            "$kind": "Microsoft.Ask",
            "activity": "@{AskLocation()}",
            "expectedProperties": [
                "Location"
            ]
        },
    ]
}
