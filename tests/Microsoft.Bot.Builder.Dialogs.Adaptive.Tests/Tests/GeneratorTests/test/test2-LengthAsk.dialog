
{
    "$schema": "https://raw.githubusercontent.com/microsoft/botbuilder-dotnet/chrimc/form/schemas/sdk.schema",
    "$kind": "Microsoft.OnAsk",
    "condition":"!$Length && contains(dialog.requiredProperties, 'Length')",
    "actions": [
        {
            "$kind": "Microsoft.Ask",
            "activity": "@{AskLength()}",
            "expectedProperties": [
                "Length"
            ]
        },
    ]
}
