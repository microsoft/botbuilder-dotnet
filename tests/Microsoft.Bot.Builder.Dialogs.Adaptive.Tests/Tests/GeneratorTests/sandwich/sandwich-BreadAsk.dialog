
{
    "$schema": "https://raw.githubusercontent.com/microsoft/botbuilder-dotnet/chrimc/form/schemas/sdk.schema",
    "$kind": "Microsoft.OnAsk",
    "condition":"!$Bread && contains(dialog.requiredProperties, 'Bread')",
    "actions": [        
        {
            "$kind": "Microsoft.Ask",
            "activity": "@{AskBread()}",
            "expectedProperties": [
                "Bread"
            ]
        }
    ]
}
