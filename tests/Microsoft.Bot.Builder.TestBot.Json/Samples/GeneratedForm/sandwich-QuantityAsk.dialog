
{
    "$schema": "https://raw.githubusercontent.com/microsoft/botbuilder-dotnet/chrimc/form/schemas/sdk.schema",
    "$kind": "Microsoft.OnEndOfActions",
    "condition":"!$Quantity && contains(dialog.requiredProperties, 'Quantity')",
    "actions": [        
        {
            "$kind": "Microsoft.Ask",
            "activity": "@{AskQuantity()}",
            "expectedProperties": [
                "Quantity"
            ]
        }
    ]
}
