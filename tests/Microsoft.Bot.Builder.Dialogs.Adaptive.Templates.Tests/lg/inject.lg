> !# @strict = false
> !# @registerNamespace = foo
> !# @registerAsFunctions = GetList, Convert, GetProperty


# Convert(index, value)
- ${setProperty(setProperty({}, "Id", index),"Topic",value)}

#GetList(tasks)
- ${indicesAndValues(tasks)}

#GetProperty
-user.tasks