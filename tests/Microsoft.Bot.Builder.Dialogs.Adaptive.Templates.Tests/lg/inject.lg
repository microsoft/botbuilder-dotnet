> !# @strict = false
> !# @namespace = foo
> !# @addToGlobalFunctionTable = bar, cool, longCondition, GetList, Convert, GetProperty

#bar()
- ${add(1,2)}

#cool(a)
- ${add(1,a)}

#longCondition(age)
- IF: ${age > 20}
- You can drive a car!
- ELSE:
- You shall not pass!

# Convert(index, value)
- ${setProperty(setProperty({}, "Id", index),"Topic",value)}

#GetList(tasks)
- ${indicesAndValues(tasks)}

#GetProperty
-user.tasks