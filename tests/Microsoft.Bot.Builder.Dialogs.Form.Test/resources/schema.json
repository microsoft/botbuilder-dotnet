{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "JSON Schema test",
  "$comment": "There is always an object at the top.  Could in theory have a mapping.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "$comment": "Explicit mapping to @@name.",
      "$mappings": [
        "@@name"
      ]
    },
    "age": {
      "type": "string",
      "$comment": "Implicit mapping to @@age"
    },
    "children": {
      "type": "array",
      "items": {
        "type":  "string"
      }
    }
  }
} 
