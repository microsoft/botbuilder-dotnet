{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$implements": [
    "Microsoft.IRule"
  ],
  "title": "Welcome Rule",
  "description": "Welcome Rule.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "steps": {
      "type": "array",
      "description": "steps to execute",
      "items": {
        "$type": "Microsoft.IDialog"
      }
    }
  },
  "required": [
    "steps"
  ]
}